# /etc/mysql/mariadb.conf.d/60-moodle.cnf
[mysqld]
bind-address = 0.0.0.0
character-set-server = utf8mb4
collation-server     = utf8mb4_unicode_ci

innodb_file_per_table = 1
innodb_large_prefix   = 1

max_allowed_packet    = 64M